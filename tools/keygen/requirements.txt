# GUI dependency for the license keygen tool.
# Install with: pip install -r tools/keygen/requirements.txt
customtkinter>=5.2.0
PyNaCl==1.6.2
# Loads the committed logo / gradient PNGs in the GUI header.
Pillow>=10.0.0
