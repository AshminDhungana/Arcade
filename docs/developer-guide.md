# Arcade Developer Guide

> TODO: Document incrementally as features are built.
>
> Key topics: local development setup, running tests, linting commands, Alembic migration commands, seed script usage, WebSocket event format reference, `agent.config.json` schema.
