- [ ] When the session is active and the timer is running, the only way to stop the timer is to conform payment and checkout, the command tab has no function and says, Remote commands and seat controls coming in a future update, i need to add remote command to pause and continue the timer as well as force overlay on and off function which is avilable in the system that can be done when sessionis not active, but should be avilable in commands so staff can do force overlay on and off from there.

- [ ] The kisko overlay should show the cafe name but it shows the word arcade in the middle, as cafe name is added during setup it should show the cafe name and if no cafe name is avilable then as a default arcade should be shown, but currently the agent only shows arcade in the overlay. Fetch cafe name from the server and then save that and show it in the center of the overlay.
