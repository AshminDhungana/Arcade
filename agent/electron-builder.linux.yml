appImage:
  artifactName: "${productName}-${version}-${arch}.${ext}"
  # AppImage integration
  # integrate: true
  # vendor: "Neurotech Biratnagar"
  # desktopFileEntries:
  #   StartupNotify: false
  #   Terminal: false
  #   Categories: "Utility;"
deb:
  artifactName: "${productName}-${version}-${arch}.${ext}"
  packageCategory: "utils"
  maintainer: "Ashmin Dhungana <ashmin.dhungana@example.com>"
  # After install script for chmod 600 on config
  afterInstall: "assets/scripts/after-install.sh"
  # Before remove script (optional)
  # beforeRemove: "assets/scripts/before-remove.sh"
# Linux targets
target:
  - target: AppImage
    arch: x64
  - target: deb
    arch: x64
